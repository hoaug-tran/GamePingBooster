# .NET
bin/
obj/
*.user
*.suo

# Go
relay/relayd
relay/relayd.arm64
relay/gpb-relay.tar.gz
relay/dist/

# Thư viện native tải về, không commit (xem client/native/wintun/README.md)
client/native/wintun/*.dll

# Bí mật: PSK, profile đã điền IP thật, cấu hình có endpoint thật
client/config.json
# Ban sao config o goc repo - xuat hien khi test, va no chua PSK that
/config.json
**/psk
*.pem
*.key

# Bản build installer
installer/output/

# Capture khi phân tích traffic game
*.pcapng
*.pcap
tools/profile-builder/.cache/
# Capture output: the addresses one machine talked to, which is that machine's browsing as much
# as it is the game's. Globs rather than two exact names, so a dated archive of one of these
# cannot slip into a repository meant to go public - which is exactly what observed.txt.bak
# nearly did on 2026-09-05.
tools/profile-builder/observed*
tools/profile-builder/landmarks-observed*
profiles/*.bak
CLAUDE.md

.vs
installer/README.md
docs/pubg-ip-ranges.md
docs/ROADMAP.md
docs/virtual-adapter.md

# Profile test cuc bo: giu endpoint that, khong public
profiles/test-stage1.json
profiles/*.local.json

# Ngu canh lam viec, khong public
HANDOFF.md
profiles/pubg-vn.json
profiles/cs2-vn.json
.gitignore

# Local machine settings for ./gpb and gpb.ps1 (relay host aliases and the like)
gpb.conf
profiles/pubg-vn.json
docs/COMMERCIAL.md

# Installer output
installer/dist/
dist/

# Cong cu va tai lieu van hanh, khong public
licence-gen
licence-gen.ps1
relay/cmd/licence-gen/
docs/LICENCE-KEYS.md

# Khoa va token sinh ra khi thu nghiem (*.key da duoc ignore o tren)
*.pub
token.hex

# Ban sao device.key / token / config truoc khi chay `gpb reset`
.reset-backup/
profiles/pubg-vn.json.bak-20260909-preRebuild
tools/profile-builder/manual-cidrs.txt
tools/Test-LicenceConnection.ps1
tools/profile-builder/tcp-sessions.txt
